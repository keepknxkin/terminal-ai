My Terminal AI assisant

A CLI tool that converts natural language into shell commands using OpenAI.

Features 
- Natural language → shell commands
- Works in CMD / PowerShell / Bash
- Simple CLI interface
Usage

```bash
python ai.py "list all files in current directory"
